export type User = {
  id: string;
  created: string;
  updated: string;
  avatar: string;
  email: string;
};

export type ContactRecord = {
  created: string;
  updated: string;
  name: string;
  email: string;
  subject: string;
  message: string;
};
